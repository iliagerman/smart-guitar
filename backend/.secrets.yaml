# Local-only backend secrets overrides.
# This file is gitignored. Fill in values for your machine.

# Database: dev uses an already-running local Postgres.
db:
  # Example:
  # url: "postgresql://postgres:postgres@localhost:5432/guitar_player"
  url: "postgresql://postgres:postgres@localhost:5432/guitar_player"

# Optional: local overrides for API keys (prefer env vars when possible)
openai:
  api_key: ""

admin:
  api-key: ""
